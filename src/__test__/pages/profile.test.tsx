/**
 * @jest-environment jsdom
 */
import { render, screen } from '@testing-library/react'
import Profile from '@/pages/profile'
import ProfileCard from '@/components/profileCard'

describe('Profile', () => {
    it('should render a profile card', async () => {
        //arrange

        //act
        
        //assert
        
    })
})